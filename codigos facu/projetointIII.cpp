# include <std.h>
#include <locale.h>

void main() {
	printf("Digite o salario: ");
	scanf("%f",&sa);
	
	printf("Digite percentual: ");
	scanf("%f", &per);
	tot = sa*per /100;
	
	tot = sa+ per 
	printf ("")
}
